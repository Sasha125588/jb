import { Typography } from '@/components/ui'

const HomePage = () => {
  return (
    <div className="h-full w-full">
      <Typography variant="display">HomePage</Typography>
    </div>
  )
}

export default HomePage
