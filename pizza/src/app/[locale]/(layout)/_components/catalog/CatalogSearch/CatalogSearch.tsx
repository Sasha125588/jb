'use client'

// import { CatalogCartDrawer } from '../CatalogCart/components/CatalogCartDrawer/CatalogCartDrawer'
// import { LogoutConfirmationModal } from '@/app/[locale]/_components/LogoutConfirmationModal/LogoutConfirmationModal'
// import { modalManager } from '@/app/[locale]/provider'
// import { Button } from '@/components/ui'
// import { useModal } from '@/shared/modal'

export const CatalogSearch = () => {
  // const onConfirm = () => console.log('URRAAA')

  // const openModal = () => LogoutConfirmationModal.open({ onConfirm })
  // const openDrawer = () => CatalogCartDrawer.open()

  // const openModal2 = () => modalManager.open('LogoutConfirmation', { onConfirm })

  // const { modal } = modalManager.useModal('CatalogCartDrawer')

  // modal?.props

  // const { modal } = useModal(LogoutConfirmationModal)

  // modal?.props.

  return (
    <>
      {/* <Button onClick={openDrawer}>open drawer</Button>
      <Button onClick={openModal}>open</Button> */}
      {/* <Button onClick={openModal2}>open2</Button> */}
      {/* <Button onClick={() => open2({ onConfirm })}>open</Button> */}
      {/* <Button onClick={() => modal?.props.onConfirm()}>onConfirm</Button> */}
      {/* <Button onClick={() => open({ onConfirm })}>open</Button> */}
    </>
  )
}
