import { Typography } from '@/components/ui'

const GamePage = () => {
  return (
    <div className="h-full w-full">
      <Typography variant="display">GamePage</Typography>
    </div>
  )
}

export default GamePage
