import { JetBrains_Mono, Nunito } from 'next/font/google'

export const nunito = Nunito({ variable: '--font-nunito', subsets: ['latin'] })
export const jetbrainsMono = JetBrains_Mono({ subsets: ['latin'], variable: '--font-jetbrains' })
